# 지시문 모음 — 「사장님이 직접 만드는 디지털 메뉴판」

AI 도구(Claude Code, ChatGPT 등)에 그대로 복사해 보내는 문장입니다. AI가 늘 그대로 따르는 것은 아니니, 바뀐 화면은 직접 열어 확인하세요.

## 지시문

### 우리 가게 분위기로 손님 화면 바꾸기
```
이 폴더의 index.html과 style.css가 우리 가게 손님용 메뉴판이야.
우리 가게는 [가게 설명: 예) 원목 가구가 많은 조용한 동네 카페]야.
style.css의 색과 글꼴만 바꿔서 분위기를 맞춰 줘. 아래는 지켜 줘.
- HTML 구조, 파일 이름, class 이름, index.html의 <script> 부분은 바꾸지 마
- 배경은 순백(#FFFFFF)·순검정(#000000) 말고, 글자는 배경과 대비 4.5:1 이상
- 본문 14px, 입력칸 16px, 모서리 둥글기는 한 계열만
- 바꾼 값을 표로 보여 줘(항목 / 전 / 후)
규칙 파일이 있으면 그 규칙도 따라 줘: https://github.com/manabout-town/afternoon-cafe/blob/main/rules/CLAUDE.md
```

### 종이 메뉴판 사진을 CSV로 바꾸기
```
첨부한 사진은 우리 가게 종이 메뉴판이야. 아래 형식의 CSV로 옮겨 줘.
첫 줄: 카테고리,이름,영문명,가격,설명
- 가격은 숫자만(쉼표·원 빼고)
- 사진에서 읽기 어려운 글자는 추측하지 말고 [확인 필요]라고 적어
- 영문명·설명은 비워 둬도 돼
- 쉼표가 들어간 칸은 큰따옴표로 감싸
```

### 영문 메뉴명 만들기
```
아래 메뉴 이름을 외국인 손님이 보는 메뉴판용 영어로 옮겨 줘.
- 한국 고유 메뉴는 발음 표기 뒤에 짧은 설명(예: Yuja Ade — citron soda)
- 과장 형용사(best, premium 등)는 넣지 마
- 결과는 "한글 이름,영문명" CSV 두 칸으로

[메뉴 이름을 한 줄에 하나씩 붙여넣기]
```

### menu-backup.js에 메뉴 추가하기
```
menu-backup.js 파일에 아래 메뉴를 추가해 줘.
- 기존 형식(window.MENU_BACKUP = { ... };)을 그대로 지키고 JSON 문법이 깨지지 않게
- category_id는 이미 있는 categories의 id를 써. 없는 종류면 categories에 새로 추가
- sort는 같은 종류 안에서 마지막 번호 다음
- 다 고친 뒤 전체 파일을 보여 줘

추가할 메뉴: [종류 / 이름 / 가격 / 설명]
```

### 에러 메시지 설명 듣기
```
메뉴판을 만드는 중에 아래 메시지가 떴어. 나는 개발자가 아니야.
1) 무슨 뜻인지 한두 문장으로
2) 내가 확인할 곳(파일 이름, Supabase 화면)을 순서대로
3) 고칠 때 비밀 키(secret, service_role)를 파일에 넣으라는 방법은 쓰지 마

[메시지 전체를 붙여넣기]
[무엇을 하다가 떴는지 한 줄]
```

### 보안 규칙 검토받기
```
아래는 우리 메뉴판 Supabase의 setup.sql이야. 보안 관점에서 검토해 줘.
- 로그인하지 않은 손님이 추가·수정·삭제할 수 있는 표가 있는지
- 로그인했지만 사장님 명단(acm_owners)에 없는 사람이 쓸 수 있는 곳이 있는지
- 발견하면 고친 SQL과 이유를 함께
단, 내가 직접 확인할 수 있게 "보안 점검 도구(tools/security-check.html)로 확인하는 법"도 알려 줘.

[setup.sql 전체를 붙여넣기]
```
